# GPW event evidence layer

PL-only, 1–2 session paper-trading module.

Primary evidence channel: issuer reports surfaced through PAP publication surfaces for ESPI/EBI. Independent news remains a separate confirmation channel. Price and volume reaction uses only the latest completed session and cannot act as a catalyst by itself. Event-family learning starts only after a minimum resolved sample and never mutates base strategy weights.
