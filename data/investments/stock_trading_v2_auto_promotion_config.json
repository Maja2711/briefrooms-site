{
  "schema_version": "stock-trading-v2-auto-promotion-config-v1",
  "automatic_production_promotion_enabled": true,
  "require_exact_replay": true,
  "require_formal_holdout_pass": true,
  "maximum_promotions_per_run": 1,
  "allowed_components": [
    "entry"
  ],
  "allowed_replay_adapters": [
    "entry_threshold_v1"
  ],
  "allowed_targets": {
    "gpw_daily_config": [
      [
        "minimum_composite_score"
      ]
    ],
    "stock_trading_policy": [
      [
        "markets",
        "GPW",
        "minimum_entry_score"
      ]
    ]
  },
  "notes": {
    "authority": "A formal fresh-holdout PASS may be materialized automatically only when an exact deployment_spec exists and still matches the current Champion revision.",
    "scope": "This is bounded deterministic config promotion, not arbitrary source-code generation.",
    "fail_closed": "Stale revisions, hash mismatches, non-allowlisted paths, unsupported adapters, or failed production invariants block promotion.",
    "evidence": "Every production change is bound to Challenger/evaluation hashes and the immutable stock-trading-v2 evidence commit."
  }
}
