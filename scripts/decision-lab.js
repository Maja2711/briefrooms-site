(()=>{"use strict";const esc=s=>String(s??"").replace(/[&<>"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;"}[m]));const pct=v=>v==null?"—":(Number(v)*100).toFixed(1)+"%";const tabs=[...document.querySelectorAll("[data-tab]")];tabs.forEach(btn=>btn.addEventListener("click",()=>{tabs.forEach(x=>x.classList.toggle("active",x===btn));document.querySelectorAll(".tab-panel").forEach(p=>p.classList.toggle("active",p.id==="tab-"+btn.dataset.tab))}));
function forecasts(items){const root=document.getElementById("forecast-list");if(!items.length){root.innerHTML='<p class="muted">Brak opublikowanych forecastów. Model nie tworzy danych demonstracyjnych.</p>';return}root.innerHTML='<div class="forecast head"><span>Entity</span><span>Belief</span><span>P</span><span>Confidence</span><span>Status</span></div>'+items.slice(0,20).map(x=>'<div class="forecast"><b>'+esc(x.entity||"—")+'</b><span>'+esc(x.belief_id||"")+'</span><span class="probability">'+pct(x.probability)+'</span><span>'+pct(x.confidence)+'</span><span>'+esc(x.status||"")+'</span></div>').join("")}
function metric(rootId,m){const root=document.getElementById(rootId);if(!root)return;const cells=[["Forecasty",m.forecast_count],["Resolved",m.resolved_count],["Eligible",m.calibration_eligible],["Brier",m.brier_score],["ECE",m.ece],["Log loss",m.log_loss]];root.innerHTML=cells.map(([k,v])=>'<div class="metric"><small>'+k+'</small><b>'+(v==null?"—":esc(v))+'</b></div>').join("")}
async function load(){try{const r=await fetch("/data/investments/decision_lab_public.json?v="+Date.now(),{cache:"no-store"});if(!r.ok)throw 0;const d=await r.json();forecasts(d.forecasts||[]);metric("metrics-summary",d.metrics||{});metric("metrics",d.metrics||{})}catch(_){forecasts([]);metric("metrics-summary",{});metric("metrics",{})}}load();})();