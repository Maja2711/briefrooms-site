(() => {
  'use strict';

  // The generated portfolio JSON is the source of truth for executed orders.
  // Do not repeatedly mutate the live dashboard, override its currency, or
  // inject additional renderers from this compatibility entry point.
})();
