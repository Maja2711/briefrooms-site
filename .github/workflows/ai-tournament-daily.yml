name: AI Tournament Daily

on:
  workflow_dispatch:
  schedule:
    # Reserved for daily valuation after the US close.
    - cron: "45 21 * * 1-5"
    # Recovery audit 90 minutes later.
    - cron: "15 23 * * 1-5"
  push:
    branches: [main]
    paths:
      - "scripts/ai_tournament_engine.py"
      - "scripts/ai_tournament_parts/**"
      - "scripts/ai-tournament-public.js"
      - "scripts/install_ai_tournament_ui.py"
      - "tests/test_ai_tournament.py"
      - "data/ai_tournament/config.json"
      - "data/ai_tournament/manual_mode.json"
      - "data/ai_tournament/commitments/**"
      - "data/ai_tournament/submissions/**"
      - ".github/workflows/ai-tournament-daily.yml"

permissions:
  contents: write

concurrency:
  group: ai-tournament-daily
  cancel-in-progress: false

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Validate repository contract
        run: |
          python -m unittest tests.test_ai_tournament -v
          python -m py_compile scripts/ai_tournament_engine.py scripts/install_ai_tournament_ui.py
          node --check scripts/ai-tournament-public.js
          node --check scripts/portfolio-10k-material-reports-public.js
          python - <<'PY'
          import json
          from pathlib import Path
          mode = json.loads(Path('data/ai_tournament/manual_mode.json').read_text(encoding='utf-8'))
          assert mode['mode'] == 'manual_locked_buy_and_hold'
          assert mode['api_calls_enabled'] is False
          print('AI Tournament manual buy-and-hold mode validated; API calls are disabled.')
          PY

  hold-until-manual-portfolios-are-locked:
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Confirm no model API is called
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          mode = json.loads(Path('data/ai_tournament/manual_mode.json').read_text(encoding='utf-8'))
          if mode.get('api_calls_enabled'):
              raise SystemExit('API calls must remain disabled for the manual tournament.')
          print('Manual submissions are being collected. No model API call and no portfolio trade is executed by this workflow yet.')
          PY
