name: Publish Section News Fast Lane (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Section fast lane paused
        run: echo "The section fast lane is paused. The layout-preserving source-only publisher now owns PL and EN news pages."
