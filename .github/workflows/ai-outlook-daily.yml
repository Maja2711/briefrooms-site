name: AI Outlook Daily — Legacy Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain replacement
        run: |
          echo "This legacy workflow is disabled."
          echo "Daily AI Outlook is published by .github/workflows/ai-outlook-gemini.yml"
