name: Update Investment Room Quotes

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/update_investment_room_quotes.py"
      - "scripts/update_investment_room_quotes_v2.py"
      - "scripts/daily_engine_contract.py"
      - "scripts/daily_engine_adapters.py"
      - "scripts/daily_eurusd_spot.py"
      - "tests/test_daily_trading_architecture.py"
      - ".github/workflows/investment-room-quotes.yml"
  schedule:
    # Avoid the busiest quarter-hour boundaries, where GitHub may delay schedules.
    - cron: "3,18,33,48 * * * *"

permissions:
  contents: write

concurrency:
  group: investment-room-quotes
  cancel-in-progress: false

jobs:
  update-investment-room-quotes:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PYTHONPATH: ${{ github.workspace }}/scripts
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependency
        run: python -m pip install --upgrade pip yfinance

      - name: Validate Daily Trading contracts
        run: |
          set -euo pipefail
          python -m unittest tests.test_daily_trading_architecture -v
          python -m py_compile scripts/daily_engine_contract.py scripts/daily_engine_adapters.py scripts/daily_eurusd_spot.py
          node --check scripts/daily-eurusd-spot-public.js

      - name: Update investment room quotes
        run: python scripts/update_investment_room_quotes_v2.py

      - name: Refresh Daily EURUSD Spot shadow engine
        run: |
          set -euo pipefail
          python scripts/daily_eurusd_spot.py --output data/investments/eurusd_daily_spot.json
          python scripts/daily_eurusd_spot.py --output data/investments/eurusd_daily_spot.json --validate
          python -m json.tool data/investments/eurusd_daily_spot.json >/dev/null

      - name: Commit investment room quotes and EURUSD shadow state
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/investments/live_prices.json data/investments/room_quotes.json data/investments/eurusd_daily_spot.json
          if git diff --cached --quiet; then
            echo "No investment room quote changes"
          else
            git commit -m "Update investment room quotes"
            git pull --rebase origin main
            git push
          fi
