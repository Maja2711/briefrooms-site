name: Legacy Emergency News Publisher (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Canonical owner notice
        run: echo "PL and EN news are owned exclusively by .github/workflows/publish-news.yml."
