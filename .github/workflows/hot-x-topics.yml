name: Update Hot X Topics

on:
  workflow_dispatch:
  schedule:
    # Three times daily. GitHub cron uses UTC: 05:17, 13:17 and 21:17.
    - cron: "17 5,13,21 * * *"
  push:
    branches: [main]
    paths:
      - "scripts/update_hot_x_3x_daily.py"
      - "scripts/build_hot_x_en.py"
      - "scripts/update_hot_x_topics.py"
      - "scripts/translate_hot_x_pl.py"
      - "scripts/validate_hot_x_feed.py"
      - "scripts/patch_hot_x_loader.py"
      - "scripts/hot-x-render.js"
      - ".github/workflows/hot-x-topics.yml"

permissions:
  contents: write

concurrency:
  group: hot-x-update
  cancel-in-progress: true

jobs:
  update-hot-x-topics:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Save last known good Hot X feed
        run: python scripts/validate_hot_x_feed.py --backup

      - name: Update Hot X automatically
        env:
          X_BEARER_TOKEN: ${{ secrets.X_BEARER_TOKEN }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          NEWS_AI_MODEL: gpt-4o-mini
        run: python scripts/update_hot_x_3x_daily.py

      - name: Final Polish-language gate
        run: python scripts/translate_hot_x_pl.py

      - name: Validate and preserve last-good Hot X cards
        run: python scripts/validate_hot_x_feed.py --validate

      - name: Load resilient Hot X renderer
        run: python scripts/patch_hot_x_loader.py

      - name: Commit updated Hot X data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/hot_tweets.json .cache pl/index.html en/index.html || true
          if git diff --cached --quiet; then
            echo "No Hot X changes to commit."
          else
            git commit -m "Update Hot X topics"
            git pull --rebase origin main
            git push
          fi
