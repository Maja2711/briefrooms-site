name: Publish PL and EN News (paused)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy publisher paused
        run: echo "The legacy atomic publisher is paused while the layout-preserving source-only publisher owns PL and EN news pages."
